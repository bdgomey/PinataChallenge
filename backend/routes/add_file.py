from fastapi import APIRouter
from models.api_key import api_key

# router = APIRouter(
#     prefix='/add',
#     tags=["add", "upload"],
    
# )

# @router.post('/')
# def post_files():
print(api_key())